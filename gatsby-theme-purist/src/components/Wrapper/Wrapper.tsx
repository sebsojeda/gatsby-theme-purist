import styled from '@emotion/styled';

const Wrapper = styled.div`
  font-size: 1.125rem;
  line-height: 1.75rem;
  max-width: 48rem;
  margin-left: auto;
  margin-right: auto;
  & > * {
    margin: 2.75rem auto;
  }
`;

export default Wrapper;
